# Optional mirrors/proxies may be supplied by the developer or CI environment.
# Official sources remain the default and integrity verification cannot be disabled.
# HTTPS_PROXY=
# HTTP_PROXY=
# NO_PROXY=
