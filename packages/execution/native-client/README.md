# @deepseek-ai/dsh-native-client

Cordis provider that launches `dsh-execution-core` without a shell and projects its JSONL protocol as `ctx.nativeExecution`. It is opt-in and does not replace `ctx.subprocess` by itself.

Model-visible impact: none. Token impact: none. KV-cache impact: none.
