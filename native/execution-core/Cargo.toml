[package]
name = "dsh-execution-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Native execution sidecar for DeepSeek Harness"

[dependencies]
base64 = "0.22"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
